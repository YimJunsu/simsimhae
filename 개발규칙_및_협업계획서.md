# simsimhae 개발 규칙 및 협업 계획서

## 문서 정보
- **프로젝트명**: simsimhae (심심해)
- **작성자**: 프로젝트 리더
- **작성일**: 2026-01-05
- **문서 버전**: 1.0
- **문서 목적**: 일관된 코드 품질 유지, 효율적인 협업 체계 구축

---

## 목차
1. [코드 작성 원칙](#1-코드-작성-원칙)
2. [커밋 및 버전 관리 규칙](#2-커밋-및-버전-관리-규칙)
3. [디렉토리 및 파일 구조 가이드](#3-디렉토리-및-파일-구조-가이드)
4. [AI 도구 활용 가이드라인](#4-ai-도구-활용-가이드라인)
5. [협업 및 커뮤니케이션 규칙](#5-협업-및-커뮤니케이션-규칙)
6. [코드 확장성 및 유지보수 계획](#6-코드-확장성-및-유지보수-계획)
7. [코드 리뷰 체크리스트](#7-코드-리뷰-체크리스트)

---

## 1. 코드 작성 원칙

### 1.1 기본 원칙
- **가독성 최우선**: 코드는 작성하는 시간보다 읽는 시간이 더 많다
- **단순성**: 복잡한 로직보다 단순하고 명확한 코드
- **일관성**: 프로젝트 전체에서 동일한 스타일 유지
- **재사용성**: DRY(Don't Repeat Yourself) 원칙 준수

### 1.2 네이밍 컨벤션

#### TypeScript/JavaScript
```typescript
// 변수명: camelCase
const userName = 'simsimhae';
const isLoggedIn = true;
const itemCount = 10;

// 함수명: camelCase, 동사로 시작
function getUserInfo() { }
function calculateSaju() { }
function handleSubmit() { }

// 클래스명: PascalCase
class UserService { }
class SajuCalculator { }

// 컴포넌트명: PascalCase
function LoginForm() { }
function SajuResultCard() { }

// 상수명: UPPER_SNAKE_CASE
const MAX_LOGIN_ATTEMPTS = 5;
const API_BASE_URL = 'https://api.simsimhae.com';

// 타입/인터페이스: PascalCase
interface User { }
type SajuResult = { };

// Private 변수/함수: _로 시작 (선택)
private _internalState = {};
```

#### 파일명
- **컴포넌트 파일**: PascalCase
  - `LoginForm.tsx`
  - `SajuResultCard.tsx`
- **유틸/헬퍼 파일**: camelCase 또는 kebab-case
  - `dateUtils.ts`
  - `saju-calculator.ts`
- **페이지 파일**: kebab-case (Next.js 규칙)
  - `index.tsx`
  - `saju.tsx`
  - `my-page.tsx`

#### 디렉토리명
- **kebab-case 사용**
  - `components/`
  - `api-routes/`
  - `common-utils/`

### 1.3 주석 작성 규칙

#### 필수 주석
```typescript
/**
 * 사주팔자를 계산하는 함수
 * @param birthDate 생년월일 (YYYY-MM-DD 형식)
 * @param birthTime 출생 시간 (HH:mm 형식)
 * @param gender 성별 ('M' | 'F')
 * @returns 사주팔자 결과 객체
 * @throws {ValidationError} 입력값이 유효하지 않을 경우
 */
function calculateSaju(
  birthDate: string,
  birthTime: string,
  gender: 'M' | 'F'
): SajuResult {
  // 구현
}
```

#### 주석 가이드라인
- **함수/클래스**: JSDoc 형식으로 작성 (필수)
- **복잡한 로직**: 왜(Why) 이렇게 작성했는지 설명
- **TODO/FIXME**: 이슈 번호와 함께 명시
  ```typescript
  // TODO(#123): 사용자 권한 체크 로직 추가 필요
  // FIXME(#456): 특정 조건에서 무한 루프 발생 가능
  ```
- **명백한 코드**: 주석 불필요 (코드 자체가 설명)
  ```typescript
  // 나쁜 예
  // 사용자 이름을 가져온다
  const name = user.name;

  // 좋은 예 (주석 없이도 명확)
  const userName = user.name;
  ```

### 1.4 코드 포맷팅

#### ESLint + Prettier 설정
```json
// .eslintrc.json
{
  "extends": [
    "next/core-web-vitals",
    "plugin:@typescript-eslint/recommended",
    "prettier"
  ],
  "rules": {
    "no-console": "warn",
    "no-unused-vars": "error",
    "@typescript-eslint/explicit-function-return-type": "warn",
    "@typescript-eslint/no-explicit-any": "error"
  }
}
```

```json
// .prettierrc
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 80,
  "arrowParens": "always"
}
```

#### 자동 포맷팅
- **저장 시 자동 포맷팅 활성화** (VS Code)
- **커밋 전 자동 포맷팅** (Husky + lint-staged)
  ```json
  // package.json
  {
    "lint-staged": {
      "*.{ts,tsx,js,jsx}": [
        "eslint --fix",
        "prettier --write"
      ]
    }
  }
  ```

### 1.5 에러 처리

#### 일관된 에러 처리
```typescript
// API 에러 처리 예시
async function fetchUserData(userId: string): Promise<User> {
  try {
    const response = await fetch(`/api/users/${userId}`);

    if (!response.ok) {
      throw new ApiError(
        `Failed to fetch user: ${response.statusText}`,
        response.status
      );
    }

    return await response.json();
  } catch (error) {
    // 에러 로깅
    logger.error('fetchUserData failed', { userId, error });

    // 사용자에게 적절한 에러 메시지 표시
    if (error instanceof ApiError) {
      throw error;
    }

    // 예상치 못한 에러
    throw new UnexpectedError('An unexpected error occurred');
  }
}
```

#### 에러 클래스 정의
```typescript
// src/lib/errors.ts
export class AppError extends Error {
  constructor(
    message: string,
    public statusCode: number = 500,
    public code?: string
  ) {
    super(message);
    this.name = this.constructor.name;
  }
}

export class ValidationError extends AppError {
  constructor(message: string) {
    super(message, 400, 'VALIDATION_ERROR');
  }
}

export class UnauthorizedError extends AppError {
  constructor(message: string = 'Unauthorized') {
    super(message, 401, 'UNAUTHORIZED');
  }
}
```

### 1.6 타입 안정성

#### TypeScript 엄격 모드 사용
```json
// tsconfig.json
{
  "compilerOptions": {
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true
  }
}
```

#### any 타입 금지
```typescript
// 나쁜 예
function processData(data: any) {
  return data.value;
}

// 좋은 예
interface DataType {
  value: string;
}

function processData(data: DataType): string {
  return data.value;
}
```

### 1.7 성능 고려사항

- **불필요한 렌더링 방지**: React.memo, useMemo, useCallback 적절히 사용
- **번들 크기 최적화**: 동적 import 사용
  ```typescript
  // 동적 import 예시
  const HeavyComponent = dynamic(() => import('./HeavyComponent'), {
    loading: () => <LoadingSpinner />,
  });
  ```
- **이미지 최적화**: Next.js Image 컴포넌트 사용
- **DB 쿼리 최적화**: N+1 문제 방지, 인덱스 활용

---

## 2. 커밋 및 버전 관리 규칙

### 2.1 커밋 메시지 규칙

#### 커밋 메시지 형식
```
[TYPE] 제목 (50자 이내)

본문 (선택사항, 72자로 줄바꿈)
- 무엇을 변경했는지
- 왜 변경했는지

이슈 번호: #123
```

#### TYPE 종류
| TYPE | 설명 | 예시 |
|------|------|------|
| `[FEAT]` | 새로운 기능 추가 | `[FEAT] AI 사주풀이 기능 구현` |
| `[FIX]` | 버그 수정 | `[FIX] 로그인 시 세션 만료 오류 수정` |
| `[DOCS]` | 문서 수정 | `[DOCS] README에 설치 방법 추가` |
| `[STYLE]` | 코드 포맷팅, 세미콜론 누락 등 | `[STYLE] Prettier 적용` |
| `[REFACTOR]` | 코드 리팩토링 (기능 변경 없음) | `[REFACTOR] 사주 계산 로직 모듈화` |
| `[TEST]` | 테스트 코드 추가/수정 | `[TEST] 음식 추천 API 테스트 추가` |
| `[CHORE]` | 빌드, 패키지 매니저 수정 | `[CHORE] 의존성 업데이트` |
| `[PERF]` | 성능 개선 | `[PERF] 이미지 lazy loading 적용` |
| `[HOTFIX]` | 긴급 버그 수정 | `[HOTFIX] 결제 오류 긴급 수정` |

#### 커밋 메시지 예시
```bash
# 좋은 예
[FEAT] 사용자 댓글 작성 기능 추가

- 댓글 CRUD API 구현
- 댓글 UI 컴포넌트 작성
- 실시간 댓글 업데이트 적용

이슈 번호: #45

# 나쁜 예
update code
fix bug
작업완료
```

### 2.2 브랜치 전략 (Git Flow 변형)

#### 브랜치 구조
```
main (배포용)
  ↑
develop (통합 개발)
  ↑
feature/기능명 (기능 개발)
hotfix/버그명 (긴급 수정)
```

#### 브랜치 명명 규칙
- **feature/**: 새로운 기능 개발
  - `feature/saju-ai-integration`
  - `feature/food-recommendation`
  - `feature/user-authentication`

- **fix/**: 버그 수정
  - `fix/login-session-error`
  - `fix/comment-delete-bug`

- **hotfix/**: 프로덕션 긴급 수정
  - `hotfix/payment-crash`

- **refactor/**: 리팩토링
  - `refactor/database-schema`

- **docs/**: 문서 작업
  - `docs/api-documentation`

#### 브랜치 작업 흐름

1. **기능 개발 시작**
   ```bash
   # develop에서 분기
   git checkout develop
   git pull origin develop
   git checkout -b feature/user-authentication
   ```

2. **작업 및 커밋**
   ```bash
   git add .
   git commit -m "[FEAT] JWT 기반 로그인 구현"
   ```

3. **원격 저장소에 푸시**
   ```bash
   git push origin feature/user-authentication
   ```

4. **Pull Request 생성**
   - GitHub/GitLab에서 PR 생성
   - develop 브랜치로 병합 요청
   - 리뷰어 지정

5. **코드 리뷰 및 병합**
   - 리뷰어 승인 후 병합
   - Squash and Merge 권장 (커밋 히스토리 정리)

6. **브랜치 삭제**
   ```bash
   # 원격 브랜치 삭제
   git push origin --delete feature/user-authentication

   # 로컬 브랜치 삭제
   git branch -d feature/user-authentication
   ```

### 2.3 Pull Request (PR) 규칙

#### PR 템플릿
```markdown
## 변경 사항
-

## 변경 이유
-

## 테스트 방법
1.
2.

## 스크린샷 (UI 변경 시)
![스크린샷](url)

## 체크리스트
- [ ] 코드 리뷰 요청
- [ ] 테스트 통과
- [ ] 문서 업데이트 (필요 시)
- [ ] 환경 변수 변경 사항 문서화 (필요 시)

## 관련 이슈
Closes #이슈번호
```

#### PR 작성 규칙
- **제목**: `[TYPE] 간결한 설명` 형식
- **본문**: 무엇을, 왜 변경했는지 명확히 작성
- **리뷰어**: 최소 1명 이상 지정
- **라벨**: 적절한 라벨 태그 (feature, bug, enhancement 등)
- **작은 단위**: 한 PR에 너무 많은 변경사항 포함 지양 (500줄 이하 권장)

#### PR 병합 조건
- [ ] 리뷰어 1명 이상 승인
- [ ] CI/CD 테스트 통과
- [ ] 충돌 해결 완료
- [ ] 코드 컨벤션 준수

### 2.4 커밋 전 체크리스트

```bash
# pre-commit hook으로 자동화
# .husky/pre-commit
#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Lint 검사
npm run lint

# 타입 체크
npm run type-check

# 테스트 실행 (선택)
npm run test

# 포맷팅
npm run format
```

### 2.5 버전 관리

#### Semantic Versioning (SemVer)
- **MAJOR.MINOR.PATCH** (예: 1.2.3)
  - **MAJOR**: 하위 호환성 없는 변경
  - **MINOR**: 하위 호환성 있는 기능 추가
  - **PATCH**: 하위 호환성 있는 버그 수정

#### 릴리스 노트 작성
```markdown
## v1.2.0 (2026-02-15)

### ✨ New Features
- AI 사주풀이 기능 추가 (#45)
- 카카오톡 공유 기능 구현 (#52)

### 🐛 Bug Fixes
- 로그인 세션 만료 오류 수정 (#48)
- 모바일 레이아웃 깨짐 현상 수정 (#50)

### 📚 Documentation
- API 문서 업데이트 (#49)

### 🔧 Chore
- 의존성 업데이트 (#51)
```

---

## 3. 디렉토리 및 파일 구조 가이드

### 3.1 프론트엔드 구조 (Next.js)

```
frontend/
├── public/                      # 정적 파일
│   ├── images/                  # 이미지 파일
│   │   ├── logo.svg
│   │   └── foods/               # 음식 이미지
│   ├── fonts/                   # 폰트 파일
│   └── icons/                   # 아이콘 파일
│
├── src/
│   ├── app/                     # Next.js 13+ App Router
│   │   ├── layout.tsx           # 루트 레이아웃
│   │   ├── page.tsx             # 메인 페이지
│   │   ├── saju/                # 사주풀이 페이지
│   │   │   ├── page.tsx
│   │   │   └── [id]/            # 동적 라우트
│   │   │       └── page.tsx
│   │   ├── food/                # 음식 추천 페이지
│   │   │   └── page.tsx
│   │   ├── auth/                # 인증 페이지
│   │   │   ├── login/
│   │   │   └── signup/
│   │   └── api/                 # API 라우트 (서버리스 함수)
│   │       ├── auth/
│   │       └── saju/
│   │
│   ├── components/              # 재사용 컴포넌트
│   │   ├── common/              # 공통 컴포넌트
│   │   │   ├── Header.tsx
│   │   │   ├── Footer.tsx
│   │   │   ├── Button.tsx
│   │   │   ├── Modal.tsx
│   │   │   └── LoadingSpinner.tsx
│   │   ├── saju/                # 사주 관련 컴포넌트
│   │   │   ├── SajuInputForm.tsx
│   │   │   ├── SajuResultCard.tsx
│   │   │   └── SajuChart.tsx
│   │   ├── food/                # 음식 관련 컴포넌트
│   │   │   ├── FoodSelector.tsx
│   │   │   ├── FoodResultCard.tsx
│   │   │   └── FoodRoulette.tsx
│   │   ├── auth/                # 인증 관련 컴포넌트
│   │   │   ├── LoginForm.tsx
│   │   │   └── SignupForm.tsx
│   │   └── comment/             # 댓글 컴포넌트
│   │       ├── CommentList.tsx
│   │       ├── CommentItem.tsx
│   │       └── CommentForm.tsx
│   │
│   ├── lib/                     # 유틸리티 함수 및 설정
│   │   ├── api/                 # API 클라이언트
│   │   │   ├── client.ts        # Axios 인스턴스
│   │   │   ├── auth.ts          # 인증 API
│   │   │   ├── saju.ts          # 사주 API
│   │   │   └── food.ts          # 음식 API
│   │   ├── utils/               # 유틸리티 함수
│   │   │   ├── dateUtils.ts
│   │   │   ├── formatUtils.ts
│   │   │   └── validation.ts
│   │   ├── hooks/               # Custom React Hooks
│   │   │   ├── useAuth.ts
│   │   │   ├── useModal.ts
│   │   │   └── useSaju.ts
│   │   ├── constants/           # 상수 정의
│   │   │   └── index.ts
│   │   └── errors/              # 에러 클래스
│   │       └── index.ts
│   │
│   ├── store/                   # 상태 관리 (Zustand)
│   │   ├── authStore.ts
│   │   ├── userStore.ts
│   │   └── index.ts
│   │
│   ├── types/                   # TypeScript 타입 정의
│   │   ├── user.ts
│   │   ├── saju.ts
│   │   ├── food.ts
│   │   ├── comment.ts
│   │   └── api.ts
│   │
│   └── styles/                  # 전역 스타일
│       ├── globals.css
│       └── themes.ts            # 테마 설정
│
├── tests/                       # 테스트 파일
│   ├── unit/                    # 단위 테스트
│   ├── integration/             # 통합 테스트
│   └── e2e/                     # E2E 테스트
│
├── .husky/                      # Git hooks
├── .vscode/                     # VS Code 설정
├── .env.local                   # 환경 변수 (로컬)
├── .env.example                 # 환경 변수 예시
├── .eslintrc.json              # ESLint 설정
├── .prettierrc                  # Prettier 설정
├── next.config.js               # Next.js 설정
├── tsconfig.json                # TypeScript 설정
├── package.json
└── README.md
```

### 3.2 백엔드 구조 (Nest.js)

```
backend/
├── src/
│   ├── main.ts                  # 엔트리 포인트
│   ├── app.module.ts            # 루트 모듈
│   │
│   ├── modules/                 # 기능별 모듈
│   │   ├── auth/                # 인증 모듈
│   │   │   ├── auth.module.ts
│   │   │   ├── auth.controller.ts
│   │   │   ├── auth.service.ts
│   │   │   ├── dto/             # Data Transfer Objects
│   │   │   │   ├── login.dto.ts
│   │   │   │   └── signup.dto.ts
│   │   │   ├── guards/          # 가드 (권한 체크)
│   │   │   │   └── jwt-auth.guard.ts
│   │   │   └── strategies/      # Passport 전략
│   │   │       └── jwt.strategy.ts
│   │   │
│   │   ├── users/               # 사용자 모듈
│   │   │   ├── users.module.ts
│   │   │   ├── users.controller.ts
│   │   │   ├── users.service.ts
│   │   │   ├── entities/        # DB 엔티티
│   │   │   │   └── user.entity.ts
│   │   │   └── dto/
│   │   │       └── create-user.dto.ts
│   │   │
│   │   ├── saju/                # 사주 모듈
│   │   │   ├── saju.module.ts
│   │   │   ├── saju.controller.ts
│   │   │   ├── saju.service.ts
│   │   │   ├── entities/
│   │   │   │   └── saju-result.entity.ts
│   │   │   └── dto/
│   │   │       └── calculate-saju.dto.ts
│   │   │
│   │   ├── food/                # 음식 추천 모듈
│   │   │   ├── food.module.ts
│   │   │   ├── food.controller.ts
│   │   │   ├── food.service.ts
│   │   │   └── entities/
│   │   │       └── food.entity.ts
│   │   │
│   │   └── comments/            # 댓글 모듈
│   │       ├── comments.module.ts
│   │       ├── comments.controller.ts
│   │       ├── comments.service.ts
│   │       └── entities/
│   │           └── comment.entity.ts
│   │
│   ├── common/                  # 공통 모듈
│   │   ├── decorators/          # 커스텀 데코레이터
│   │   │   └── user.decorator.ts
│   │   ├── filters/             # 예외 필터
│   │   │   └── http-exception.filter.ts
│   │   ├── interceptors/        # 인터셉터
│   │   │   └── logging.interceptor.ts
│   │   ├── pipes/               # 파이프 (유효성 검사)
│   │   │   └── validation.pipe.ts
│   │   └── middleware/          # 미들웨어
│   │       └── logger.middleware.ts
│   │
│   ├── config/                  # 설정 파일
│   │   ├── database.config.ts
│   │   ├── jwt.config.ts
│   │   └── app.config.ts
│   │
│   └── utils/                   # 유틸리티
│       ├── saju-calculator.ts
│       ├── date-converter.ts
│       └── ai-client.ts         # AI API 클라이언트
│
├── test/                        # 테스트 파일
│   ├── unit/
│   ├── integration/
│   └── e2e/
│
├── migrations/                  # DB 마이그레이션
├── seeds/                       # DB 시드 데이터
├── .env                         # 환경 변수
├── .env.example
├── nest-cli.json
├── tsconfig.json
├── package.json
└── README.md
```

### 3.3 파일 명명 규칙

#### 컴포넌트 파일
```typescript
// 컴포넌트와 같은 이름의 파일
// components/common/Button.tsx
export default function Button() { }

// index.ts로 re-export (선택)
// components/common/index.ts
export { default as Button } from './Button';
export { default as Modal } from './Modal';
```

#### 서비스/유틸 파일
```typescript
// kebab-case 또는 camelCase
// lib/utils/date-utils.ts 또는 dateUtils.ts
export function formatDate() { }
```

#### 타입 파일
```typescript
// 기능별로 그룹화
// types/user.ts
export interface User { }
export type UserRole = 'admin' | 'user';

// types/index.ts에서 re-export
export * from './user';
export * from './saju';
```

### 3.4 페이지 파일 구조 규칙

#### 페이지 디렉토리 구성
페이지 파일은 `page` 디렉토리 안에 기능별/카테고리별로 구성합니다.

```
src/
├── page/                        # 페이지 파일 전용 디렉토리
│   ├── mainPage.tsx             # 메인 페이지
│   ├── 사주/                    # 사주 관련 페이지
│   │   ├── 사주메인.tsx         # 사주 메인 페이지
│   │   ├── 사주입력.tsx         # 사주 입력 페이지
│   │   └── 사주결과.tsx         # 사주 결과 페이지
│   ├── 음식/                    # 음식 추천 관련 페이지
│   │   ├── 음식메인.tsx
│   │   └── 음식결과.tsx
│   ├── 회원/                    # 회원 관련 페이지
│   │   ├── 로그인.tsx
│   │   ├── 회원가입.tsx
│   │   └── 마이페이지.tsx
│   └── 공통/                    # 공통 페이지
│       ├── 이용약관.tsx
│       └── 개인정보처리방침.tsx
```

#### 페이지 파일 명명 규칙
- **파일명**: PascalCase 또는 한글 명사형
  - `mainPage.tsx` 또는 `메인페이지.tsx`
  - `사주메인.tsx`, `사주입력.tsx`, `사주결과.tsx`
- **디렉토리명**: 기능명 (한글 또는 영문)
  - `사주/`, `음식/`, `회원/`
  - 또는 `saju/`, `food/`, `auth/`

#### 페이지 컴포넌트 작성 예시
```typescript
// page/사주/사주메인.tsx
import React from 'react';
import SajuInputForm from '@/components/saju/SajuInputForm';

/**
 * 사주 메인 페이지
 * 사용자로부터 생년월일시를 입력받는 페이지
 */
export default function 사주메인() {
  return (
    <div className="saju-main-page">
      <h1>AI 사주풀이</h1>
      <SajuInputForm />
    </div>
  );
}
```

```typescript
// page/mainPage.tsx
import React from 'react';
import Link from 'next/link';

/**
 * 메인 페이지
 * 사이트의 랜딩 페이지
 */
export default function MainPage() {
  return (
    <div className="main-page">
      <h1>심심해 (simsimhae)</h1>
      <nav>
        <Link href="/saju">AI 사주풀이</Link>
        <Link href="/food">뭐먹지?</Link>
      </nav>
    </div>
  );
}
```

#### 페이지와 컴포넌트 분리 원칙
- **페이지 (`page/`)**: 라우팅, 레이아웃, 데이터 패칭 담당
- **컴포넌트 (`components/`)**: 재사용 가능한 UI 로직 담당

```typescript
// ✅ 좋은 예: 페이지는 레이아웃과 데이터 패칭만
// page/사주/사주메인.tsx
export default function 사주메인() {
  const { user } = useAuth();

  return (
    <PageLayout title="AI 사주풀이">
      <SajuInputForm user={user} />  {/* 실제 로직은 컴포넌트에 */}
    </PageLayout>
  );
}

// ❌ 나쁜 예: 페이지에 비즈니스 로직 포함
export default function 사주메인() {
  const [birthDate, setBirthDate] = useState('');
  const handleSubmit = () => {
    // 복잡한 로직...
  };

  return (
    <div>
      {/* JSX... */}
    </div>
  );
}
```

#### 페이지 임포트 규칙
```typescript
// 절대 경로 사용 권장
import MainPage from '@/page/mainPage';
import 사주메인 from '@/page/사주/사주메인';

// 상대 경로는 가급적 지양
import MainPage from '../../page/mainPage';  // ❌
```

### 3.5 디렉토리 구조 원칙

1. **기능별 분리**: 기능 단위로 디렉토리 구성
2. **계층 구조**: 3-4단계 깊이 유지 (너무 깊지 않게)
3. **공통 코드 분리**: 재사용 가능한 코드는 common 또는 shared에
4. **확장 가능**: 새로운 기능 추가 시 기존 구조에 쉽게 추가 가능
5. **명확한 책임**: 각 디렉토리/파일은 명확한 하나의 책임

---

## 4. AI 도구 활용 가이드라인

### 4.1 AI 활용 가능 영역

#### ✅ 적극 활용
1. **코드 자동 생성**
   - 보일러플레이트 코드 (CRUD, 폼 등)
   - 타입 정의 생성
   - 테스트 코드 템플릿

2. **문서 작성**
   - JSDoc 주석 자동 생성
   - README 초안 작성
   - API 문서 작성

3. **코드 리뷰 보조**
   - 잠재적 버그 탐지
   - 성능 개선 제안
   - 보안 취약점 점검

4. **리팩토링 지원**
   - 코드 구조 개선 제안
   - 네이밍 개선
   - 중복 코드 제거

5. **테스트 작성**
   - 단위 테스트 생성
   - 엣지 케이스 도출
   - 모킹 코드 작성

6. **UI/UX 아이디어**
   - 디자인 제안
   - 레이아웃 구조 제안
   - 접근성 개선 제안

### 4.2 AI 활용 워크플로우

```mermaid
AI 코드 생성
    ↓
개발자 리뷰
    ↓
코드 이해 및 수정
    ↓
테스트 작성/실행
    ↓
코드 리뷰 (동료)
    ↓
커밋/배포
```

### 4.3 AI 사용 시 주의사항

#### ⚠️ 필수 원칙
1. **AI 생성 코드는 반드시 검토 후 사용**
   - 코드 동작 완전히 이해
   - 보안 취약점 확인
   - 프로젝트 컨벤션 준수 확인

2. **맹목적 복사 금지**
   ```typescript
   // ❌ 나쁜 예: AI 코드 그대로 복사
   // AI가 생성한 복잡한 코드를 이해 없이 사용

   // ✅ 좋은 예: AI 코드를 이해하고 수정
   // AI 제안을 참고하여 프로젝트에 맞게 재작성
   ```

3. **의존도 최소화**
   - 핵심 로직은 직접 작성
   - AI는 보조 도구로만 활용

4. **저작권 및 라이선스 확인**
   - AI가 생성한 코드의 출처 확인
   - 오픈소스 라이선스 준수

### 4.4 추천 AI 도구

#### 코드 작성
- **GitHub Copilot**: 실시간 코드 제안
- **ChatGPT/Claude**: 복잡한 로직 설명 및 구현
- **Cursor**: AI 통합 IDE

#### 코드 리뷰
- **CodeRabbit**: PR 자동 리뷰
- **SonarQube**: 코드 품질 분석

#### 문서화
- **Mintlify**: 자동 문서 생성
- **Notion AI**: 문서 작성 보조

#### 테스트
- **GitHub Copilot**: 테스트 코드 생성
- **Diffblue**: Java 단위 테스트 자동 생성

### 4.5 AI 활용 예시

#### 예시 1: CRUD 보일러플레이트 생성
```typescript
// AI에게 요청
// "NestJS에서 User CRUD API 생성해줘"

// AI 생성 코드 (예시)
@Controller('users')
export class UsersController {
  constructor(private readonly usersService: UsersService) {}

  @Post()
  create(@Body() createUserDto: CreateUserDto) {
    return this.usersService.create(createUserDto);
  }
  // ...
}

// 개발자 검토 및 수정
// 1. DTO 검증 규칙 추가
// 2. 에러 처리 추가
// 3. 인증 가드 추가
```

#### 예시 2: 주석 자동 생성
```typescript
// AI에게 요청: "이 함수에 JSDoc 주석 추가해줘"

/**
 * 사주팔자를 계산하는 함수
 * @param birthDate - 생년월일 (YYYY-MM-DD)
 * @param birthTime - 출생 시간 (HH:mm)
 * @returns 사주팔자 결과
 */
function calculateSaju(birthDate: string, birthTime: string) {
  // 구현
}
```

#### 예시 3: 테스트 코드 생성
```typescript
// AI에게 요청: "calculateSaju 함수 테스트 코드 작성해줘"

describe('calculateSaju', () => {
  it('올바른 입력에 대해 사주 결과를 반환해야 함', () => {
    const result = calculateSaju('1990-01-01', '10:00');
    expect(result).toBeDefined();
    expect(result.year).toBeDefined();
  });

  it('잘못된 날짜 형식에 대해 에러를 던져야 함', () => {
    expect(() => calculateSaju('invalid', '10:00')).toThrow();
  });

  // 개발자가 엣지 케이스 추가
});
```

#### 예시 4: React + Tailwind 웹사이트 틀 생성

**AI에게 요청하는 방법**
```
다음 요구사항을 만족하는 React + Tailwind CSS 반응형 웹사이트 틀을 만들어줘.

프로젝트: simsimhae (심심해)

1. 전체 레이아웃
   - 데스크탑에서는 화면 중앙에 최대 1200px 폭으로 콘텐츠 배치
   - 화면 양쪽은 여백으로 처리
   - 모바일에서는 화면 폭에 맞게 자동으로 축소
   - 모든 화면에서 헤더, 본문, 푸터 포함
   - Next.js App Router 구조 사용

2. 헤더
   - 사이트 로고 "심심해" 좌측, 메뉴 우측 배치 (AI 사주풀이, 뭐먹지?, 로그인)
   - 모바일에서는 햄버거 메뉴로 변경
   - 스크롤 시 헤더 상단 고정 (sticky)

3. 본문
   - 히어로 섹션: 메인 타이틀 + 설명
   - 카드형 콘텐츠 2개 배치 (사주풀이, 음식추천 각 카드)
   - 데스크탑: 가로 2열
   - 태블릿/모바일: 가로 1열로 변경
   - 카드에는 아이콘, 제목, 설명, 시작하기 버튼 포함

4. 푸터
   - 저작권 문구와 간단한 링크 (이용약관, 개인정보처리방침)
   - 모바일에서도 가독성 유지

5. 디자인 톤
   - 심플, 깔끔, 재미있는 느낌
   - 컬러: 메인 컬러 (보라/파랑 계열), 서브 컬러 (밝은 톤)
   - 여백 충분히, 글자 크기 명확히
   - 다크모드 지원 (선택사항)

6. 기술 스택
   - React 18+
   - Next.js 14+ App Router
   - Tailwind CSS
   - TypeScript
   - 반응형 디자인 (모바일 우선)
   - 접근성 고려 (시맨틱 HTML, ARIA 레이블)

7. 컴포넌트 구조
   - components/common/Header.tsx
   - components/common/Footer.tsx
   - components/main/HeroSection.tsx
   - components/main/FeatureCard.tsx
   - app/page.tsx (메인 페이지)

코드는 프로젝트 컨벤션에 맞게:
- 컴포넌트는 PascalCase
- 파일 구조는 기능별 분리
- JSDoc 주석 포함
- PropTypes는 TypeScript interface로 정의
```

**AI 생성 코드 예시**

```typescript
// components/common/Header.tsx
'use client';

import { useState } from 'react';
import Link from 'next/link';

/**
 * 사이트 헤더 컴포넌트
 * - 로고, 네비게이션 메뉴 포함
 * - 모바일에서 햄버거 메뉴로 전환
 */
export default function Header() {
  const [isMenuOpen, setIsMenuOpen] = useState(false);

  return (
    <header className="sticky top-0 z-50 bg-white shadow-md dark:bg-gray-900">
      <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <div className="flex h-16 items-center justify-between">
          {/* 로고 */}
          <Link href="/" className="text-2xl font-bold text-purple-600">
            심심해
          </Link>

          {/* 데스크탑 메뉴 */}
          <nav className="hidden md:flex md:space-x-8">
            <Link
              href="/saju"
              className="text-gray-700 hover:text-purple-600 transition"
            >
              AI 사주풀이
            </Link>
            <Link
              href="/food"
              className="text-gray-700 hover:text-purple-600 transition"
            >
              뭐먹지?
            </Link>
            <Link
              href="/login"
              className="text-gray-700 hover:text-purple-600 transition"
            >
              로그인
            </Link>
          </nav>

          {/* 모바일 햄버거 버튼 */}
          <button
            onClick={() => setIsMenuOpen(!isMenuOpen)}
            className="md:hidden p-2 rounded-md text-gray-700 hover:bg-gray-100"
            aria-label="메뉴 열기"
          >
            <svg
              className="h-6 w-6"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              {isMenuOpen ? (
                <path
                  strokeLinecap="round"
                  strokeLinejoin="round"
                  strokeWidth={2}
                  d="M6 18L18 6M6 6l12 12"
                />
              ) : (
                <path
                  strokeLinecap="round"
                  strokeLinejoin="round"
                  strokeWidth={2}
                  d="M4 6h16M4 12h16M4 18h16"
                />
              )}
            </svg>
          </button>
        </div>

        {/* 모바일 메뉴 */}
        {isMenuOpen && (
          <nav className="md:hidden py-4 space-y-2">
            <Link
              href="/saju"
              className="block px-4 py-2 text-gray-700 hover:bg-gray-100 rounded"
            >
              AI 사주풀이
            </Link>
            <Link
              href="/food"
              className="block px-4 py-2 text-gray-700 hover:bg-gray-100 rounded"
            >
              뭐먹지?
            </Link>
            <Link
              href="/login"
              className="block px-4 py-2 text-gray-700 hover:bg-gray-100 rounded"
            >
              로그인
            </Link>
          </nav>
        )}
      </div>
    </header>
  );
}
```

```typescript
// components/common/Footer.tsx
import Link from 'next/link';

/**
 * 사이트 푸터 컴포넌트
 * - 저작권 정보 및 링크 포함
 */
export default function Footer() {
  return (
    <footer className="bg-gray-100 dark:bg-gray-800 mt-16">
      <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-8">
        <div className="flex flex-col md:flex-row md:justify-between items-center space-y-4 md:space-y-0">
          {/* 저작권 */}
          <p className="text-gray-600 dark:text-gray-400 text-sm">
            © 2026 심심해 (simsimhae). All rights reserved.
          </p>

          {/* 링크 */}
          <div className="flex space-x-6">
            <Link
              href="/terms"
              className="text-gray-600 hover:text-purple-600 text-sm transition"
            >
              이용약관
            </Link>
            <Link
              href="/privacy"
              className="text-gray-600 hover:text-purple-600 text-sm transition"
            >
              개인정보처리방침
            </Link>
            <Link
              href="/contact"
              className="text-gray-600 hover:text-purple-600 text-sm transition"
            >
              문의하기
            </Link>
          </div>
        </div>
      </div>
    </footer>
  );
}
```

```typescript
// components/main/HeroSection.tsx
/**
 * 메인 페이지 히어로 섹션
 * - 사이트 소개 및 주요 메시지 전달
 */
export default function HeroSection() {
  return (
    <section className="bg-gradient-to-r from-purple-500 to-blue-500 text-white py-20">
      <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 text-center">
        <h1 className="text-4xl md:text-6xl font-bold mb-4">
          심심할 때, 심심해!
        </h1>
        <p className="text-xl md:text-2xl mb-8 text-purple-100">
          AI 사주풀이부터 랜덤 음식 추천까지, 재미있는 컨텐츠가 가득
        </p>
        <div className="flex flex-col sm:flex-row gap-4 justify-center">
          <a
            href="/saju"
            className="bg-white text-purple-600 px-8 py-3 rounded-lg font-semibold hover:bg-purple-50 transition"
          >
            사주 보러가기
          </a>
          <a
            href="/food"
            className="border-2 border-white text-white px-8 py-3 rounded-lg font-semibold hover:bg-white hover:text-purple-600 transition"
          >
            뭐먹지? 고민하기
          </a>
        </div>
      </div>
    </section>
  );
}
```

```typescript
// components/main/FeatureCard.tsx
import Link from 'next/link';

interface FeatureCardProps {
  title: string;
  description: string;
  icon: string;
  href: string;
  color: 'purple' | 'blue' | 'green';
}

/**
 * 메인 페이지 기능 카드 컴포넌트
 * @param title - 카드 제목
 * @param description - 카드 설명
 * @param icon - 아이콘 (이모지)
 * @param href - 링크 URL
 * @param color - 카드 컬러 테마
 */
export default function FeatureCard({
  title,
  description,
  icon,
  href,
  color
}: FeatureCardProps) {
  const colorClasses = {
    purple: 'from-purple-500 to-purple-600 hover:from-purple-600 hover:to-purple-700',
    blue: 'from-blue-500 to-blue-600 hover:from-blue-600 hover:to-blue-700',
    green: 'from-green-500 to-green-600 hover:from-green-600 hover:to-green-700',
  };

  return (
    <Link href={href}>
      <div className={`
        bg-gradient-to-br ${colorClasses[color]}
        text-white rounded-2xl p-8 shadow-lg
        hover:shadow-2xl transform hover:-translate-y-2
        transition-all duration-300 cursor-pointer
        h-full
      `}>
        {/* 아이콘 */}
        <div className="text-6xl mb-4">{icon}</div>

        {/* 제목 */}
        <h3 className="text-2xl font-bold mb-3">{title}</h3>

        {/* 설명 */}
        <p className="text-purple-100 mb-6">{description}</p>

        {/* 버튼 */}
        <div className="inline-flex items-center text-white font-semibold">
          시작하기
          <svg
            className="ml-2 w-5 h-5"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              strokeLinecap="round"
              strokeLinejoin="round"
              strokeWidth={2}
              d="M9 5l7 7-7 7"
            />
          </svg>
        </div>
      </div>
    </Link>
  );
}
```

```typescript
// app/page.tsx
import Header from '@/components/common/Header';
import Footer from '@/components/common/Footer';
import HeroSection from '@/components/main/HeroSection';
import FeatureCard from '@/components/main/FeatureCard';

/**
 * 메인 페이지
 * - 사이트 랜딩 페이지
 */
export default function MainPage() {
  return (
    <div className="min-h-screen flex flex-col">
      <Header />

      <main className="flex-grow">
        {/* 히어로 섹션 */}
        <HeroSection />

        {/* 주요 기능 카드 */}
        <section className="py-16 px-4 sm:px-6 lg:px-8">
          <div className="mx-auto max-w-7xl">
            <h2 className="text-3xl font-bold text-center mb-12 text-gray-900 dark:text-white">
              무엇을 해볼까요?
            </h2>

            <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
              <FeatureCard
                title="AI 사주풀이"
                description="생년월일시를 입력하면 AI가 당신의 사주를 분석해드립니다. 재물운, 애정운, 건강운까지 한눈에!"
                icon="🔮"
                href="/saju"
                color="purple"
              />

              <FeatureCard
                title="뭐먹지?"
                description="점심, 저녁, 간식... 고민되시나요? 랜덤으로 추천해드립니다. 오늘의 메뉴는 바로 이것!"
                icon="🍔"
                href="/food"
                color="blue"
              />
            </div>
          </div>
        </section>

        {/* 최근 활동 섹션 (선택사항) */}
        <section className="bg-gray-50 dark:bg-gray-900 py-16 px-4 sm:px-6 lg:px-8">
          <div className="mx-auto max-w-7xl">
            <h2 className="text-3xl font-bold text-center mb-8 text-gray-900 dark:text-white">
              최근 활동
            </h2>
            <p className="text-center text-gray-600 dark:text-gray-400">
              곧 업데이트 예정입니다!
            </p>
          </div>
        </section>
      </main>

      <Footer />
    </div>
  );
}
```

```typescript
// app/layout.tsx
import type { Metadata } from 'next';
import './globals.css';

export const metadata: Metadata = {
  title: '심심해 - 심심할 때 찾는 재미있는 컨텐츠',
  description: 'AI 사주풀이, 랜덤 음식 추천 등 다양한 컨텐츠를 제공합니다.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="ko">
      <body className="antialiased">{children}</body>
    </html>
  );
}
```

```css
/* app/globals.css */
@tailwind base;
@tailwind components;
@tailwind utilities;

/* 커스텀 스크롤바 (선택사항) */
@layer utilities {
  .scrollbar-hide::-webkit-scrollbar {
    display: none;
  }

  .scrollbar-hide {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
}
```

```javascript
// tailwind.config.js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './pages/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    './app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#f5f3ff',
          100: '#ede9fe',
          200: '#ddd6fe',
          300: '#c4b5fd',
          400: '#a78bfa',
          500: '#8b5cf6',
          600: '#7c3aed',
          700: '#6d28d9',
          800: '#5b21b6',
          900: '#4c1d95',
        },
      },
    },
  },
  plugins: [],
  darkMode: 'class', // 다크모드 지원
};
```

**개발자 검토 및 수정 사항**
1. ✅ 접근성 개선: ARIA 레이블 추가, 시맨틱 HTML 사용
2. ✅ 타입 안정성: TypeScript interface 정의
3. ✅ 반응형 디자인: Tailwind 반응형 클래스 활용
4. 🔄 이미지 최적화: Next.js Image 컴포넌트로 교체 필요
5. 🔄 SEO: 메타 태그 추가 필요
6. 🔄 성능: 코드 스플리팅 적용 필요

### 4.6 AI 활용 시 체크리스트

코드를 프로젝트에 반영하기 전:
- [ ] 코드를 완전히 이해했는가?
- [ ] 보안 취약점은 없는가?
- [ ] 프로젝트 컨벤션을 준수하는가?
- [ ] 테스트를 작성했는가?
- [ ] 성능에 문제는 없는가?
- [ ] 필요한 주석을 추가했는가?
- [ ] 불필요한 코드는 제거했는가?

---

## 5. 협업 및 커뮤니케이션 규칙

### 5.1 Git 기반 협업

#### 브랜치 전략 요약
```
main          [배포용 안정 버전]
  ↑
develop       [통합 개발 브랜치]
  ↑
feature/*     [기능별 개발 브랜치]
```

#### 협업 워크플로우
1. **이슈 생성**: GitHub Issues에 작업 내용 등록
2. **브랜치 생성**: `feature/이슈번호-기능명`
3. **개발 및 커밋**: 작은 단위로 자주 커밋
4. **PR 생성**: develop으로 병합 요청
5. **코드 리뷰**: 동료 리뷰어 지정
6. **수정 반영**: 리뷰 피드백 반영
7. **병합**: 리뷰 승인 후 Squash Merge
8. **브랜치 삭제**: 병합 후 feature 브랜치 삭제

### 5.2 코드 리뷰 프로세스

#### 리뷰어 가이드
- **24시간 내 리뷰**: PR 생성 후 1영업일 내 피드백
- **건설적 피드백**: 비판이 아닌 개선 제안
- **질문하기**: 이해되지 않는 부분은 질문
- **칭찬하기**: 좋은 코드에는 긍정적 피드백

#### 리뷰 체크포인트
- [ ] 코드가 요구사항을 충족하는가?
- [ ] 가독성이 좋은가?
- [ ] 테스트가 포함되어 있는가?
- [ ] 보안 취약점은 없는가?
- [ ] 성능 문제는 없는가?
- [ ] 문서화가 적절한가?

#### 리뷰 코멘트 예시
```markdown
# 좋은 리뷰 코멘트
💡 제안: 이 부분은 useMemo를 사용하면 성능이 개선될 것 같습니다.
❓ 질문: 이 로직이 특정 케이스를 고려한 건가요?
👍 좋아요: 에러 처리가 명확하네요!

# 나쁜 리뷰 코멘트
❌ 이거 잘못됐어요.
❌ 이렇게 하면 안되죠.
```

### 5.3 커뮤니케이션 채널

#### 채널별 용도
| 채널 | 용도 | 응답 시간 |
|------|------|-----------|
| **GitHub Issues** | 버그 리포트, 기능 요청 | 1-2일 |
| **GitHub PR** | 코드 리뷰, 기술 논의 | 1일 |
| **Slack/Discord** | 빠른 질문, 일상 소통 | 1-2시간 |
| **Notion** | 문서, 회의록, 기획 | - |
| **정기 회의** | 주간 스프린트, 회고 | 매주 |

#### Slack/Discord 채널 구성 (예시)
- `#general`: 일반 공지 및 잡담
- `#dev-frontend`: 프론트엔드 개발 논의
- `#dev-backend`: 백엔드 개발 논의
- `#qa`: QA 및 버그 리포트
- `#deploy`: 배포 알림
- `#monitoring`: 에러 알림, 모니터링

### 5.4 회의 규칙

#### 주간 스프린트 미팅 (매주 월요일)
- **시간**: 30분
- **안건**:
  - 지난주 완료 사항 공유
  - 이번주 목표 설정
  - 블로커 논의
- **산출물**: 회의록 (Notion)

#### 코드 리뷰 미팅 (필요 시)
- **시간**: 30-60분
- **안건**: 복잡한 PR에 대한 집중 리뷰

#### 회고 (격주)
- **시간**: 1시간
- **형식**: KPT (Keep, Problem, Try)
- **목적**: 프로세스 개선

### 5.5 문서화 규칙

#### 필수 문서
1. **README.md**: 프로젝트 소개, 설치 방법
2. **CONTRIBUTING.md**: 기여 가이드
3. **API 문서**: Swagger/Postman
4. **회의록**: Notion에 주간 기록
5. **결정 사항 로그**: ADR (Architecture Decision Record)

#### ADR 템플릿
```markdown
# ADR-001: Next.js를 프론트엔드 프레임워크로 선택

## 상태
승인됨

## 컨텍스트
프론트엔드 프레임워크 선택 필요

## 결정
Next.js 14 사용

## 이유
- SSR/SSG 지원으로 SEO 최적화
- React 기반으로 생태계 풍부
- Vercel 배포 간편

## 결과
- 초기 로딩 속도 개선
- 개발 생산성 향상

## 날짜
2026-01-05
```

### 5.6 이슈 관리

#### 이슈 템플릿 (버그)
```markdown
## 버그 설명
간단한 버그 설명

## 재현 단계
1.
2.
3.

## 예상 동작
무엇이 일어나야 하는가?

## 실제 동작
무엇이 일어났는가?

## 스크린샷
(있다면)

## 환경
- OS:
- 브라우저:
- 버전:
```

#### 이슈 라벨 체계
- `bug`: 버그
- `feature`: 새 기능
- `enhancement`: 기능 개선
- `documentation`: 문서
- `good first issue`: 초보자 적합
- `priority:high`: 높은 우선순위
- `priority:low`: 낮은 우선순위

---

## 6. 코드 확장성 및 유지보수 계획

### 6.1 확장 가능한 아키텍처 원칙

#### SOLID 원칙 준수
1. **단일 책임 원칙 (SRP)**
   ```typescript
   // ❌ 나쁜 예: 여러 책임
   class UserService {
     createUser() { }
     sendEmail() { }
     logActivity() { }
   }

   // ✅ 좋은 예: 책임 분리
   class UserService {
     createUser() { }
   }
   class EmailService {
     sendEmail() { }
   }
   class LogService {
     logActivity() { }
   }
   ```

2. **개방-폐쇄 원칙 (OCP)**
   ```typescript
   // 확장에는 열려있고, 수정에는 닫혀있어야 함
   interface PaymentProcessor {
     process(amount: number): void;
   }

   class CreditCardPayment implements PaymentProcessor {
     process(amount: number) { }
   }

   class KakaoPayPayment implements PaymentProcessor {
     process(amount: number) { }
   }
   ```

3. **의존성 역전 원칙 (DIP)**
   ```typescript
   // 구체적인 구현이 아닌 인터페이스에 의존
   class SajuService {
     constructor(
       private aiClient: AIClient  // 인터페이스
     ) {}
   }
   ```

### 6.2 모듈화 전략

#### 기능별 모듈 분리
```typescript
// ✅ 각 기능을 독립된 모듈로
modules/
  ├── auth/        # 인증 모듈
  ├── saju/        # 사주 모듈
  ├── food/        # 음식 모듈
  └── comments/    # 댓글 모듈

// 각 모듈은 독립적으로 테스트/배포 가능
```

#### 공통 코드 재사용
```typescript
// lib/common/
export function validateEmail(email: string): boolean {
  // 모든 모듈에서 재사용
}
```

### 6.3 데이터베이스 확장성

#### 마이그레이션 관리
```typescript
// migrations/2026-01-05-create-users-table.ts
export async function up(db: Database) {
  await db.schema.createTable('users', (table) => {
    table.increments('id').primary();
    table.string('email').unique().notNullable();
    table.timestamps(true, true);
  });
}

export async function down(db: Database) {
  await db.schema.dropTable('users');
}
```

#### 인덱스 최적화
```sql
-- 자주 조회되는 컬럼에 인덱스
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_saju_results_user_id ON saju_results(user_id);
```

### 6.4 API 버전 관리

#### URL 기반 버저닝
```typescript
// v1 API
@Controller('api/v1/users')
export class UsersV1Controller { }

// v2 API (하위 호환성 없는 변경)
@Controller('api/v2/users')
export class UsersV2Controller { }
```

#### 응답 형식 표준화
```typescript
// 성공 응답
{
  "success": true,
  "data": { ... },
  "message": "Success"
}

// 에러 응답
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Invalid email format"
  }
}
```

### 6.5 테스트 전략

#### 테스트 피라미드
```
       /\
      /E2E\       10% (느림, 비용 높음)
     /------\
    /통합테스트\    20%
   /----------\
  /  단위테스트  \  70% (빠름, 비용 낮음)
 /--------------\
```

#### 테스트 커버리지 목표
- **전체**: 80% 이상
- **핵심 로직**: 90% 이상
- **유틸리티**: 100%

#### 테스트 예시
```typescript
// 단위 테스트
describe('calculateSaju', () => {
  it('should calculate saju correctly', () => {
    const result = calculateSaju('1990-01-01', '10:00');
    expect(result).toBeDefined();
  });
});

// 통합 테스트
describe('SajuController', () => {
  it('POST /saju should return 201', async () => {
    const response = await request(app)
      .post('/api/saju')
      .send({ birthDate: '1990-01-01', birthTime: '10:00' });

    expect(response.status).toBe(201);
  });
});

// E2E 테스트
describe('Saju Feature', () => {
  it('should allow user to get saju result', async () => {
    await page.goto('/saju');
    await page.fill('#birthDate', '1990-01-01');
    await page.click('#submit');
    await expect(page.locator('.result')).toBeVisible();
  });
});
```

### 6.6 성능 최적화 체크리스트

#### 프론트엔드
- [ ] 이미지 최적화 (WebP, lazy loading)
- [ ] 코드 스플리팅 (dynamic import)
- [ ] 불필요한 리렌더링 방지 (React.memo)
- [ ] 번들 크기 최적화 (tree shaking)
- [ ] CDN 활용

#### 백엔드
- [ ] DB 쿼리 최적화 (N+1 방지)
- [ ] 캐싱 전략 (Redis)
- [ ] API 응답 압축 (gzip)
- [ ] 페이지네이션 구현
- [ ] DB 커넥션 풀 관리

### 6.7 모니터링 및 로깅

#### 필수 모니터링 항목
```typescript
// 에러 로깅
logger.error('User login failed', {
  userId: user.id,
  error: error.message,
  timestamp: new Date(),
});

// 성능 측정
const start = performance.now();
await heavyOperation();
const duration = performance.now() - start;
logger.info('Operation completed', { duration });
```

#### 알림 설정
- 서버 다운: 즉시 알림
- 에러율 > 1%: 10분 내 알림
- 응답 시간 > 2초: 30분 내 알림

### 6.8 보안 체크리스트

- [ ] SQL Injection 방지 (ORM 사용)
- [ ] XSS 방지 (입력값 sanitize)
- [ ] CSRF 방지 (토큰 사용)
- [ ] Rate Limiting (과도한 요청 방지)
- [ ] 환경 변수 관리 (.env)
- [ ] HTTPS 강제
- [ ] 보안 헤더 설정 (Helmet.js)
- [ ] 정기 의존성 업데이트

### 6.9 UX/UI 확장 전략

#### 모바일 우선 설계
```css
/* 모바일 기본 */
.container {
  width: 100%;
  padding: 16px;
}

/* 태블릿 */
@media (min-width: 768px) {
  .container {
    max-width: 720px;
  }
}

/* 데스크톱 */
@media (min-width: 1024px) {
  .container {
    max-width: 1200px;
  }
}
```

#### 접근성 (a11y)
- [ ] 의미있는 HTML 태그 사용 (`<button>`, `<nav>` 등)
- [ ] 키보드 네비게이션 지원
- [ ] ARIA 라벨 추가
- [ ] 색상 대비 비율 준수 (WCAG 2.1)
- [ ] 스크린 리더 테스트

---

## 7. 코드 리뷰 체크리스트

### 7.1 기능성
- [ ] 요구사항을 충족하는가?
- [ ] 모든 엣지 케이스를 처리하는가?
- [ ] 에러 처리가 적절한가?

### 7.2 코드 품질
- [ ] 가독성이 좋은가?
- [ ] 변수/함수명이 명확한가?
- [ ] 중복 코드가 없는가?
- [ ] 주석이 적절히 작성되었는가?

### 7.3 성능
- [ ] 불필요한 연산이 없는가?
- [ ] 메모리 누수 가능성은 없는가?
- [ ] DB 쿼리가 최적화되었는가?

### 7.4 보안
- [ ] 입력값 검증이 되는가?
- [ ] 보안 취약점은 없는가?
- [ ] 민감 정보가 노출되지 않는가?

### 7.5 테스트
- [ ] 단위 테스트가 작성되었는가?
- [ ] 테스트 커버리지가 충분한가?
- [ ] 통합 테스트가 필요한가?

### 7.6 문서화
- [ ] 함수 주석이 작성되었는가?
- [ ] README 업데이트가 필요한가?
- [ ] API 문서 업데이트가 필요한가?

---

## 부록

### A. 유용한 VS Code 확장

- **ESLint**: 코드 린팅
- **Prettier**: 코드 포맷팅
- **GitLens**: Git 히스토리 시각화
- **Thunder Client**: API 테스트 (Postman 대체)
- **Error Lens**: 에러 인라인 표시
- **Auto Rename Tag**: HTML 태그 자동 rename
- **Path Intellisense**: 경로 자동완성
- **Todo Tree**: TODO 주석 관리

### B. Git 유용한 명령어

```bash
# 커밋 히스토리 예쁘게 보기
git log --oneline --graph --decorate --all

# 특정 파일의 변경 이력
git log -p <파일명>

# 작업 중인 변경사항 임시 저장
git stash
git stash pop

# 브랜치 간 특정 커밋 가져오기
git cherry-pick <커밋해시>

# 마지막 커밋 메시지 수정
git commit --amend

# 로컬 브랜치 정리
git branch --merged | grep -v '\*\|main\|develop' | xargs -n 1 git branch -d
```

### C. 참고 자료

- [Google TypeScript Style Guide](https://google.github.io/styleguide/tsguide.html)
- [Airbnb JavaScript Style Guide](https://github.com/airbnb/javascript)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [Git Flow](https://nvie.com/posts/a-successful-git-branching-model/)

### D. 용어집

- **PR**: Pull Request
- **LGTM**: Looks Good To Me (리뷰 승인)
- **WIP**: Work In Progress (작업 중)
- **YAGNI**: You Ain't Gonna Need It (필요 없을 기능 미리 구현 X)
- **DRY**: Don't Repeat Yourself (중복 배제)
- **KISS**: Keep It Simple, Stupid (단순하게)

---

## 변경 이력

| 버전 | 날짜 | 변경 내용 | 작성자 |
|------|------|-----------|--------|
| 1.0 | 2026-01-05 | 초안 작성 | 프로젝트 리더 |

---

**이 문서는 프로젝트 진행에 따라 지속적으로 업데이트됩니다.**

**모든 팀원은 이 규칙을 숙지하고 준수해야 합니다.**

**문의사항이 있을 경우 팀 리더에게 연락 바랍니다.**